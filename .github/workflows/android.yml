name: Build Android APK (User)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: npm ci

      - name: Build React app
        run: npm run build

      # الخطوة الجديدة: تغيير معرف التطبيق إلى معرف المستخدم
      - name: Set User App ID
        run: |
          sed -i 's/"appId": "[^"]*"/"appId": "${{ secrets.USER_APP_ID }}"/' capacitor.config.json
          # إذا كنت تستخدم capacitor.config.ts بدلاً من json، استخدم الأمر التالي:
          # sed -i "s/appId: '[^']*'/appId: '${{ secrets.USER_APP_ID }}'/" capacitor.config.ts

      - name: Add Android platform
        run: npx cap add android

      - name: Sync Capacitor
        run: npx cap sync android

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Build APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: user-app-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
